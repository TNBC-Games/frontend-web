import * as types from "../"

// export const signupUser = (data) => async (dispatch) => {
//     dispatch({types: types.REGULAR_SIGNUP_STARTED})
//     try{
//         const response = true
        
//     }
//}